DISPLAY THOSE JOBS FOR WHICH COMPANY HAS HIRED EMPLOYEES IN BOTH 1996 AND 1998 BUT NOT IN 1999.
ans->


DISPLAY THOSE WHOSE JOB_TITLE IS Stock Manager
ANS-> SELECT FIRST_NAME
      FROM EMPLOYEES
      WHERE JOB_ID IN (SELECT JOB_ID FROM JOBS WHERE JOB_TITLE = 'Stock Manager');

DISPLAY THOSE WHO REPORTS TO Peter's MANAGER
ANS->SELECT FIRST_NAME
     FROM EMPLOYEES
     WHERE DEPARTMENT_ID = (SELECT DEPARTMENT_ID FROM EMPLOYEES WHERE  FIRST_NAME = 'Peter');

DISPLAY THOSE JOB_ID FOR WHICH EMPLOYEES WERE HIRED UNTIL YEAR 95, BUT NOT AFTER THAT
ANS-> SELECT JOB_ID FROM EMPLOYEES WHERE TO_CHAR(HIRE_DATE,'YY') < (SELECT TO_CHAR(HIRE_DATE,'YY')  FROM EMPLOYEES WHERE TO_CHAR(HIRE_DATE,'YY') = '95');


DISPLAY FIRST_NAME WHO RESIDES IN Canada. 
ANS->SELECT FIRST_NAME FROM EMPLOYEES JOIN DEPARTMENTS USING(DEPARTMENT_ID) WHERE LOCATION_ID=(SELECT LOCATION_ID FROM LOCATIONS WHERE COUNTRY_ID=(SELECT COUNTRY_ID FROM COUNTRIES WHERE COUNTRY_NAME='Canada'));

DISPLAY GRADE_LEVEL IN WHICH NO EMPLOYEE IS GETTING SALARY.
ANS->


DISPLAY LAST_NAME AND MANAGER_ID WHO REPORTS TO King
ANS->SELECT LAST_NAME FROM EMPLOYEES WHERE DEPARTMENT_ID = (SELECT DEPARTMENT_ID FROM EMPLOYEES WHERE LAST_NAME = 'King');

DISPLAY THE JOB_ID AND NO. OF EMPLOYEES FOR THE JOB, FOR WHICH LOWEST NO. OF EMPLOYEES WORKS IN THE COMPANY
ANS->

DISPLAY THOSE WHO DOES SAME JOB AS Diana
ans-> SELECT FIRST_NAME FROM EMPLOYEES WHERE JOB_ID =(SELECT JOB_ID FROM EMPLOYEES WHERE FIRST_NAME ='Diana');

DISPLAY THOSE WHOSE GETS SALARY IN GRADE_LEVEL 'D' 
ANS-> SELECT FIRST_NAME FROM EMPLOYEES WHERE SALARY BETWEEN ( SELECT  LOWEST_SAL FROM JOB_GRADES WHERE GRADE_LEVEL ='D') AND ( SELECT  HIGHEST_SAL FROM JOB_GRADES WHERE GRADE_LEVEL ='D');

DISPLAY HIGHEST SALARY QUARTER WISE (IGNORE YEAR) 
ANS->  SELECT MAX(SALARY*12)/3 FROM EMPLOYEES;

DISPLAY MANAGER_ID , COUNT(*) FOR THE MANAGER_ID TO WHOM HIGHEST NO. OF EMPLOYEES REPORT
ANS->SELECT MANAGER_ID,COUNT(*) FROM EMPLOYEES WHERE MANAGER_ID > (SELECT COUNT(MANAGER_ID) FROM EMPLOYEES) GROUP BY MANAGER_ID;

DISPLAY FIRST_NAME , MANAGER_ID , SALARY , AVERAGE SALARY PAID FOR MANAGER_ID AND DIFFERENCE
ANS->

DISPLAY DEPARTMENT_ID , MANAGER_ID FOR THOSE EMPLOYEES WHO REPORT TO HIS DEPARTMENT HEAD
ANS->  SELECT DEPARTMENT_ID , MANAGER_ID FROM EMPLOYEES WHERE DEPARTMENT_ID = (SELECT MAX(DEPARTMENT_ID) FROM EMPLOYEES);

DEVELOP ENAME AND EMPNO AS FIRST RESULT SET. DNAME AS SECOND RESULT SET AND CLUB BOTH VERTICALLY
ANS->

DISPLAY EMPLOYEE_ID , HIRE YEAR , SALARY , LOWEST SALARY , HIGHEST SALARY FOR THE HIRE YEAR
ANS-> SELECT EMPLOYEE_ID , TO_CHAR(HIRE_DATE,'YY') , SALARY, MIN(SALARY),MAX(SALARY*12) FROM EMPLOYEES GROUP BY EMPLOYEE_ID , TO_CHAR(HIRE_DATE,'YY') , SALARY;

DISPLAY ALL DEPARTMENT_NAME AND CITY IN SINGLE COLUMN (ONE ABOVE THE OTHER) WITH PROPER COLUMN HEADING.
ANS-> 

DISPLAY JOB_ID , AVERAGE SALARY FOR THE JOB_ID WHICH GETS LOWEST AVERAGE SALARY
ANS->SELECT  JOB_ID ,AVG(SALARY) FROM EMPLOYEES WHERE  SALARY < (SELECT  AVG(SALARY) FROM  EMPLOYEES) GROUP BY JOB_ID;

DISPLAY FIRST_NAME AND SALARY OF THE EMPLOYEES WHO GET HIGHER SALARY THAN THE AVERAGE SALARY OF COMPANY.
ANS-> SELECT   FIRST_NAME, SALARY FROM EMPLOYEES WHERE SALARY > (SELECT AVG(SALARY) FROM EMPLOYEES) GROUP BY FIRST_NAME, SALARY;

DISPLAY THOSE WHO WERE HIRED IN SAME YEAR IN WHICH Kimerely HIRED
ANS->  SELECT FIRST_NAME FROM EMPLOYEES WHERE TO_CHAR(HIRE_DATE,'YY') = ( SELECT TO_CHAR(HIRE_DATE,'YY') FROM EMPLOYEES WHERE FIRST_NAME = 'Kimerely');